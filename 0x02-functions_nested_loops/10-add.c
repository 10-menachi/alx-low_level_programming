#include <stdio.h>
#include "main.h"
/**
 * add - returns the sum of two numbers
 * @a: first number
 * @b: second number
 *
 * Return: sum (Sum of the parametes given)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
