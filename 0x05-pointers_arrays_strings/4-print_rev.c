#include "main.h"

/**
 * print_rev - prints a string in reverse
 * followed by a new line
 * @s: given string
 */

void print_rev(char *s)
{
	while (*str)
		*str++;
	while (*str)
		_putchar(*str++);
	_putchar(10);
}
