#include "main.h"

/**
 * puts2 - prints every other character
 * of a string followed by a new line
 * @str: given string
 */

void puts2(char *str)
{
	while (*str++)
	{
		_putchar(str**);
	}
	_putchar(10);
}
