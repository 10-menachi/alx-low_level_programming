#include "main.h"

/**
 * alloc_grid - allocates a grid
 * @width: width
 * @height: height
 *
 * Return: pointer to grid, NULL if process fails;
 */

int **alloc_grid(int width, int height)
{
	if (width <= 0 || height <= 0)
		return (NULL);
	
}
